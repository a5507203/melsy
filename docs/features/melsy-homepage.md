# Melsy 官网 V2 Feature Spec

## 1. Concept Brief 来源

本规格来自 `PRODUCT.md`、用户确认的 V2 信息架构，以及 `C:/Users/Admin/Downloads/7月墨悉官网v2/` 中的首页、产品、场景、合作、公司与研究材料。

- 当前阶段：需求已确认，进入实现。
- 目标用户：低空安全/园区/企业决策者为主；科研合作、产业伙伴、投资媒体与技术人才为辅。
- 核心问题：旧站以产品展示为中心，无法先建立“协同世界模型”的品类认知，也没有形成技术—产品—应用—证据—联系的完整说服链。
- MVP：中文响应式静态站；一个核心首页，两个二级内容页；本地图片与网页优化视频；明确联系入口。
- 非目标：CMS、后台、账号、在线购买、提交式表单、完整英文站。

## 2. 统一语言

| 术语 | 定义 | 使用约束 |
|---|---|---|
| 协同世界模型 | 在共享状态、资源约束和因果演化下，为异构智能体生成协调决策与动作的技术范式 | 不等同于“多个智能体并行运行” |
| 空间训练场 | 面向低空空间任务的仿真、训练与验证环境 | 页面角色为 Train / 训练 |
| COS 协同操作系统 | 连接世界模型与多智能体执行，完成感知融合、任务编排与实时调度 | 页面角色为 Decide / 决策与调度 |
| 碎蜂 | 面向低空安全任务的硬件执行与验证系统 | 页面角色为 Execute / 执行；不展示未经批准的性能指标 |
| 低空产业核心落地 | 协同世界模型当前的商业切入场景 | 不代表公司长期能力只限于低空安全 |
| 产业合作 | 文档明确记录的联合研发、测试验证或行业协作 | 不把素材库中的 Logo 自动解释为客户关系 |

禁用或待审批口径：全球首个、世界级、90+%、300km/h、精准制导，以及仅存在于融资 PPT 的估值、收入、竞品与路线图内容。

## 3. 业务规则表

| ID | 触发条件 | 约束与允许行为 | 用户可见结果 | 验收场景 |
|---|---|---|---|---|
| BR-1 | 用户进入首页 | 先解释协同世界模型，再展示产品；不得以孤立产品卡开场 | 首屏明确“是什么、为什么重要、下一步做什么” | AS-1, AS-2 |
| BR-2 | 展示三项产品 | 必须表达训练→决策调度→执行验证的闭环；不得重复建设另一套产品区 | 用户能区分三者角色及关系 | AS-3 |
| BR-3 | 展示公开证据 | 只使用网站文档明确内容；PPT-only 商业数据不上站；强口径不展示 | 页面无未经批准的数字或关系暗示 | AS-4, AS-5 |
| BR-4 | 用户浏览应用 | 固定展示四类场景，文案与图片一一对应 | 用户可快速判断业务相关性 | AS-4 |
| BR-5 | 用户寻找更多信息 | About 与 Research 使用独立静态页面；全站导航可达且可返回首页 | 内容层级清楚、没有死路 | AS-6 |
| BR-6 | 视频可播放 | 静音、提供控制、poster 与文字降级；失败时不得留下空白 | 有无视频播放都能理解产品 | AS-7 |
| BR-7 | 移动端、键盘或减弱动态环境 | 导航、内容与 CTA 必须可访问；不得依赖 hover 或动画表达关键信息 | 关键路径在受限环境仍完整 | AS-8 |

## 4. 用户流程

### 主流程

1. 访客打开首页，在首屏理解协同世界模型与墨悉定位。
2. 继续阅读范式演进和核心机制，理解它区别于单智能体/并行多智能体。
3. 通过空间训练场、COS、碎蜂理解训练—决策—执行闭环。
4. 浏览四类应用和产业合作，判断相关性与可信度。
5. 点击“商务合作”，到达联系区或联系页面信息。

### 替代流程

- 从导航直接进入产品、应用或产业合作锚点。
- 从首页进入 About 或 Research，阅读后通过全站导航回到首页或联系入口。
- 从论文/研究条目打开已提供的外部论文链接。

### 异常与恢复流程

- 视频加载/播放失败：显示 poster 与完整文字说明，并允许继续浏览。
- JavaScript 不可用：核心内容、页面链接与锚点仍可阅读和导航。
- 减弱动态：停用自动播放与装饰动画，不影响信息结构。

## 5. 领域模型影响

本项目是无后端、无数据库的静态网站，没有 schema、迁移、事务或 API 变化。

内容实体包括：页面、页面区块、产品、应用场景、合作案例、公司动态、研究成果和联系入口。HTML 是公开内容投影；本地媒体文件是展示资源；Markdown 规格与源文档是内容权威来源。

## 6. 边界场景

- 缺失/损坏图片或视频。
- 小屏幕、超长中文标题、横竖屏切换。
- 键盘导航、焦点可见性、移动菜单展开与关闭。
- `prefers-reduced-motion` 环境。
- 外部论文链接不可达时，站内介绍仍保留完整上下文。
- 无 JavaScript 时核心信息可读。
- 未获批准的 Logo 或强口径不得因素材存在而自动上线。

## 7. Acceptance Scenarios

- **AS-1** Given 首次访客打开首页，When 首屏完成渲染，Then 可看到“协同世界模型：具身智能时代的新范式”、一段说明、商务合作主操作和产品 Demo 次操作。
- **AS-2** Given 访客进入理论区，When 阅读范式对比与核心机制，Then 能区分单智能体、多智能体并行与协同智能，并能通过键盘操作可展开内容。
- **AS-3** Given 访客进入产品区，When 依次浏览三个产品，Then 能理解空间训练场负责训练、COS 负责决策调度、碎蜂负责执行验证。
- **AS-4** Given 访客评估适用性，When 浏览应用区，Then 能看到四类明确场景及其对应图片和能力说明，且不出现禁用强口径。
- **AS-5** Given 访客评估可信度，When 浏览产业合作、About 和 Research，Then 能看到文档明确的合作、公司与研究信息，不出现 PPT-only 融资数据。
- **AS-6** Given 访客使用桌面或移动导航，When 在首页、About、Research 与联系入口间跳转，Then 每个目标可达、当前状态可理解且无死链。
- **AS-7** Given 视频无法播放或用户暂停视频，When 查看产品媒体，Then poster 和文字仍能完整说明产品，且控制按钮状态正确。
- **AS-8** Given 用户使用小屏、键盘或减弱动态设置，When 完成核心浏览路径，Then 内容不溢出、焦点可见、菜单可关闭、关键内容不依赖动画。

## 8. 非目标确认

- 不建设 CMS、后台、表单提交、账号、在线购买或完整英文站。
- 不新增融资、估值、收入预测、竞品对比、路线图或未经批准的指标。
- 不把所有素材库 Logo 解释为客户或合作关系。
- 不在本次建设三项产品的独立详情页；Section 3 是统一产品入口。

## Design Plan

1. **变更目标**：把旧单页的“产品优先”结构改为“品类—机制—产品—场景—证据—联系”，并新增 About、Research 两个阅读页面。
2. **影响域**：接口层为 `index.html`、`about.html`、`research.html`；共享视觉和行为下沉到 `styles.css`、`site.js`；本地媒体进入 `assets/v2/`。不引入后端或数据层。
3. **放置位置**：页面保留各自内容；导航、菜单、视频控制和视觉 token 共享。静态站规模不足以引入组件框架或构建系统。
4. **依赖方向**：HTML 依赖共享 CSS/JS 和本地资产；共享 JS 不依赖具体页面 DOM 之外的可选增强，缺失模块时安全跳过。
5. **模型边界**：没有 API/domain/storage 模型；Markdown 与源文档定义内容边界，HTML 只做公开投影。
6. **替代方案**：继续单文件内联 CSS/JS，改动更少但会在三个页面重复样式与行为，放弃；引入 React/Vite 会增加当前不需要的构建依赖，也放弃。
7. **验证方式**：Node 静态集成测试、资源和禁用文案扫描、本地 HTTP 浏览、桌面/移动截图、键盘与 reduced-motion 手动验证。

## 不变量台账

| 不变量 | 权威数据源 | 锁/事务 | API 投影 | 前端状态 | 回归测试 |
|---|---|---|---|---|---|
| 首页先解释技术范式，再展示三产品闭环 | 本 Feature Spec 与 V2 文案 | 不适用：静态只读 | 不适用：无 API | HTML 章节顺序为权威；JS 只增强 | `tests/site-smoke.mjs` 章节/文案断言；浏览器主流程 |
| 公开页面不得出现禁用强口径或 PPT-only 商业数据 | 本规格禁用词与网站源文档 | 不适用 | 不适用 | 静态 HTML 内容 | 禁用词扫描；人工内容复核 |
| 所有页面与本地媒体引用可达 | 仓库文件树与 HTML 引用 | 不适用 | 不适用 | 导航菜单、锚点、视频 poster | 资源存在检查；本地 HTTP 导航验证 |
| 视频失败或减弱动态时内容仍完整 | `DESIGN.md` 与产品文字 | 不适用 | 不适用 | poster、控制状态、reduced-motion CSS/JS | 静态属性检查；浏览器禁用动画/视频手测 |
| 键盘与移动端可完成核心路径 | `DESIGN.md` 可访问性约束 | 不适用 | 不适用 | 菜单展开状态、焦点、锚点 | 语义/ARIA 静态检查；键盘与移动视口验证 |
